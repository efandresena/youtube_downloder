yt-dlp>=2024.1.0
aiohttp>=3.9.0

# PyGObject/GTK/libadwaita should be installed with your Linux distribution's
# package manager, not pip, because it depends on system GObject introspection
# libraries. See README.md for distro-specific package names.
